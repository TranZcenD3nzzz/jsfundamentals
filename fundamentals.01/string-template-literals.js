// string concatenation
const firstName = 'John'; //camelCase, PascalCase, snake_case, kebab-case
const lastName = 'Doe';

const fullName = lastName + " " + firstName;
console.log('fullName', fullName);

// string template literals
const send = "send";
const gasoline = "gasoline gas";
const random = "escape metal play fierce";
console.log("--string template literals--")
const longDescription = `salt ${send} sand greater ${random} ${gasoline}`;
console.log('longDescription', longDescription);

